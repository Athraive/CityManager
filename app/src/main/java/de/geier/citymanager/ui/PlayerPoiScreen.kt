package de.geier.citymanager.ui

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun PlayerPoiScreen(
    categoryId: String
) {
    Text("PlayerPoiScreen – TODO (categoryId = $categoryId)")
}
