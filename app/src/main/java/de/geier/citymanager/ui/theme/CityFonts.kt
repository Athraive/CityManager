package de.geier.citymanager.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import de.geier.citymanager.R

val ScifiFont = FontFamily(
    Font(R.font.orbitron_regular)
)

val MedievalFont = FontFamily(
    Font(R.font.vinque_regular)
)

val WesternFont = FontFamily(
    Font(R.font.roboto_slab_regular)
)

val AsiaFont = FontFamily(
    Font(R.font.noto_serif_jp_regular)
)